console.log("in Utils.js")

const name = 'Sachin';
const add = function (a,b) {
    return a + b;
}

// NOTE: you need to write here what you want to be assigned.
module.exports = {
    add,
    name
};